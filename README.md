# standard_ros_interfaces
Report default ROS interfaces within network

## standard ros topics

This tool will fetch all packages from the kinetic release ( http://www.ros.org/browse/list.php ), compare existing topic types with these packages and perform a status report.

    ./standard_ros_topic_types
